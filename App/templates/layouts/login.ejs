<!DOCTYPE html>

<head>
    <title>RSS de SA de CV</title>
    <link rel="stylesheet" href="/static/css/styles.css">
</head>

<body>
    <%- include('../partials/topbar', { active: 'dashboard' , user: user }) %>
        <div class="main">
            <div class="container" id="form">
                <p>Log in:</p>
                <% if (typeof error !=='undefined' && error) { %>
                    <div class="error">
                        <%= error %>
                    </div>
                    <% } %>
                        <form id="userForm" autocomplete="off" method="POST" action="/login">
                            <label for="name">Nombre:</label><br>
                            <input type="text" name="name" id="name" autocomplete="off"><br><br>
                            <label for="password">Contraseña:</label><br>
                            <input type="password" name="password" id="password" autocomplete="off"><br><br>
                            <input type="submit" id="submit" value="Iniciar sesión">
                        </form>
            </div>
        </div>
        <!-- <script src="/static/js/app.js"></script> -->
</body>
<%- include('../partials/footer', { active: 'dashboard' }) %>